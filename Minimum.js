/* In this exercise we're supposed to create a function that shows the smallest of two numbers */

function minimum(x, y) {
    
    if (x > y) {
      return y;
    }
    
    else {
      return x;
    }
}
